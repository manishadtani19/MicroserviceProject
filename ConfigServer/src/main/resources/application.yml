server:
  port: 8085

spring:
  application:
    name: CONFIG-SERVER

  cloud:
    config:
      server:
        git:
          uri: https://github.com/manishadtani19/Microservice-Config-Server-File.git
          clone-on-start: true